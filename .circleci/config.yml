version: 2

jobs:
  build:
    docker:
      - image: newlogic42/circleci-android:latest
    steps:
      - checkout
      - run: git submodule update --init
      - run:
          name: "Initial"
          command: |
              if test -f "build.sh"; then
                  ./build.sh
              fi
      - store_artifacts:
          path: /home/circleci/project/build/idpassapi/jniLibs.tar.bz2
